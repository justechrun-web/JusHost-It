// This file can be used to set up any global state or configuration
// needed before running the test suite. For example, mocking external
// services or setting up test-specific environment variables.

console.log("Setting up test environment...");

// Example: Mocking an external service
// global.myExternalService = {
//   fetchData: () => Promise.resolve({ success: true }),
// };

console.log("Test environment setup complete.");
